import ClubsCatalog from "./components/catalog/catalog";

type Props = {};

const ClubsPage = (props: Props) => {
    return (
        <div className="-mt-[80px] md:-mt-[150px] ">
            <ClubsCatalog />
        </div>
    );
};

export default ClubsPage;
