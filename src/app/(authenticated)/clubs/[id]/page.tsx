import ClubPage from "@/components/pages/club/clubPage";
import React from "react";

type Props = {};

const Club = (props: Props) => {
    return (
        <div>
            <ClubPage />
        </div>
    );
};

export default Club;
